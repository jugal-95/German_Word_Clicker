/**
 * config.example.js
 *
 * Copy this file to "config.js" (same folder) and paste in your own free
 * Gemini API key. config.js is gitignored so your key never gets committed.
 *
 * How to get a free key (no credit card required):
 *   1. Go to https://aistudio.google.com/apikey
 *   2. Sign in with any Google account
 *   3. Click "Create API key"
 *   4. Paste it below
 *
 * Free tier covers far more lookups than you'll realistically need for
 * subtitle-watching — see the README for current limits.
 */

const GWC_CONFIG = {
  GEMINI_API_KEY: "PASTE_YOUR_KEY_HERE",
};
